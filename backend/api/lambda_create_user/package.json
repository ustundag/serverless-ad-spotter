{
  "name": "lambda_create_user",
  "version": "1.0.0",
  "description": "Handles POST requests and inserts user data to RDS.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "github.com/ustundag",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^2.2.5"
  }
}
